#include <stdio.h>
/**
 * main - a program that print the line using the printf function command in c
 * Return: 0 (completed)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
